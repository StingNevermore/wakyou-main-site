# 多语言文案编辑说明

- 日文、中文、英文文案分别位于 `ja.json`、`zh.json`、`en.json`。
- 只修改冒号右侧的文本，不要修改键名、引号、逗号或括号。
- 三个文件必须保持相同的键和数组结构；新增文案时需要同时补齐三种语言。
- 页面标题中的 `\\|` 是 Vue I18n 所需的转义写法，请保留反斜杠。
- 修改后运行 `npm test`，可检查 JSON 格式和三种语言的结构是否一致。
